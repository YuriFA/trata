# Plan: run `/opsx:propose` for `mobile-dashboard-filtered-queries`

Planning artifacts only — no project code edits. I'll run `openspec new change "mobile-dashboard-filtered-queries"`, then create the spec-driven artifacts (proposal.md → specs delta → design.md → tasks.md), validating each with `openspec status`. All content below is verified against the current code.

## Verified codebase facts (research done)

- `transactionsInMonth`/`MonthCursor` now live in **`packages/dates/src/month.ts`** (recent dates migration). The request's `shared/lib/calendar/month.ts` no longer exists → the helper belongs in `packages/dates` (shared with web later; not on the do-not-touch list). Recorded as an adaptation.
- `TransactionQuery` already has `type`/`categoryId`/`fromDate`/`toDate` (`CalendarDay` = `YYYY-MM-DD`). SQLite repo semantics: `fromDate` = inclusive midnight UTC, `toDate` = inclusive end of UTC day (`<= T23:59:59.999Z`). Mock repo honors all filters → existing dashboard tests exercise the filtered path and stay green.
- `CategoryExpensesSheet` has its **own in-sheet month navigator** (own cursor, reset to `initialCursor` on each present) → it needs its own filtered query keyed to the sheet cursor, otherwise month navigation breaks once the main query is month-bounded.
- `SummaryCard`, `AllExpensesCard`, `CategorySection` derive from `(cursor, transactions, categories)` props via selectors — work on any month superset; props unchanged. `expenses-sheet` has no in-sheet navigation.
- Mutations invalidate the `['transactions']` key prefix — already covers all filtered variants.

## Artifacts to create

### 1. `proposal.md`
Why: dashboard loads full history via `useTransactions()` and slices in JS; web will copy this pattern onto HTTP soon. What: month-bounded dashboard query, category sheet's own filtered query, product decision pinning "selected month" = device-local calendar month. Scope: mobile + `packages/dates` helper only; no backend/OpenAPI/`packages/api`/web changes; no sheet UI changes (data plumbing only).

### 2. `specs/mobile-local-data/spec.md` — delta
**MODIFIED requirement: Home screen data behavior** — add the product decision: the "selected month" is the device's local calendar month (matching current `transactionsInMonth` behavior, previously undefined). New scenario: a transaction at 00:30 local on the 1st belongs to the new month. Kept product-level — no SQL-vs-JS-filter implementation detail in the spec.

### 3. `design.md`
- `monthToUtcDayRange(cursor)` in `packages/dates/src/month.ts` (+ export in index): local month start/end instants → inclusive UTC days `fromDate`/`toDate` that are a **superset** of the local month (via `toISOString().slice(0,10)`; UTC+3 → fromDate = last day of prev month; UTC−5 → toDate = 1st of next month). Dates package must not import `@trata/api` — returns plain `{ fromDate: string; toDate: string }`, structurally matching `TransactionQuery`.
- `dashboard-screen.tsx`: `useTransactions(monthToUtcDayRange(cursor))`; children props unchanged (selectors trim any superset).
- `CategoryExpensesSheet`: own `useTransactions({ type: 'expense', categoryId, ...monthToUtcDayRange(sheetCursor) }, { enabled: !!category })`; result trimmed by existing `expenseDayGroups`/`totalExpenses` (exact local semantics → converges with `categoryBreakdown`); derivation wrapped in `useMemo`; drop the now-unused `transactions` prop (data plumbing only, UI untouched).
- `use-transactions.ts`: second optional param `{ enabled?: boolean }` passed to `useQuery`. Invalidation unchanged.
- Tests: existing dashboard/hook tests must pass unedited; new unit tests for the helper at month boundaries (UTC+3, UTC−5, 31-day month, Dec→Jan wrap) with `process.env.TZ` save/restore in mobile's jest (dates package has no test runner; `apps/mobile/src/shared/lib/dates.test.ts` is the established home for dates-package coverage).
- Noted trade-off: brief empty-state flash on sheet month switch while the local query resolves (SQLite-fast; `keepPreviousData` left as a future option for HTTP).

### 4. `tasks.md`
Ordered steps: 1) helper + unit tests (packages/dates + mobile test), 2) `useTransactions` enabled param, 3) month-bounded dashboard query, 4) category sheet filtered query + memoized derivation + prop cleanup, 5) full verification (`pnpm type-check`, mobile jest suite, lint) and spec-delta validation.

## Out of scope (recorded in proposal)
Backend, OpenAPI, `packages/api`, web, `transactions-screen.tsx` (same pattern, not in this change), sheet UI redesigns.

After artifacts are written I'll run `openspec status` to confirm everything is ready, summarize, and stop — implementation waits for an explicit `/opsx:apply`.